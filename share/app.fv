import signal

class Signals:
  def __init self:
    def connect:
      signal.signal(signal.SIGINT, self.intHandler)
    end

    connect
  end

  def intHandler self, signum, frame:
    p "exit app"
    exit()
  end
end

signals = Signals
signal.pause()
